# Work trail — living product vs required

**This is the operational tracker.** Update it at the end of every agent session that ships, blocks, or changes direction. Do not leave status only in chat.

| Field | Value |
| --- | --- |
| **Last updated** | 2026-08-21 (P10 Contract Portfolio + Renewal/Rebid, and the P1–P10 final audit) |
| **Git HEAD on origin/main** | _(parent verifies then commits)_ |
| **Product truth gate** | VERIFY9 = READY WITH NONBLOCKING LIMITATIONS — **not** full Phase 2–8 exit, and **not** production-ready. Live packages **22**; **15** A/B packages fully VERIFIED against an exit of ~20–30, so the Historical Pilot has **not** exited. VERIFY 5 / 6 / 7 currently return **verdict FAIL** on stale fixtures versus the Phase 9 trust triggers — see [PRODUCTIZATION_P1_P10_FINAL_AUDIT.md](productization/PRODUCTIZATION_P1_P10_FINAL_AUDIT.md). VERIFY2B **8/8**, VERIFY8 **23/23**. Local Ask synthesis: **Ollama** (free) + Google Gemini fallback; Gateway needs card; OpenAI key quota-blocked. Secrets stay in `.env.local` only. **Commercialization (Stripe / MCP / agents) is not started and is not a core product phase.** |
| **Rollback** | `cursor-phase2-foundation` @ `8d2d031` |

Companion: [CURRENT_STATE_AUDIT.md](CURRENT_STATE_AUDIT.md). Blueprint: [MASTER_BLUEPRINT.md](MASTER_BLUEPRINT.md). Long-form domains/tables: [MASTER_PRODUCT_CONTEXT.md](MASTER_PRODUCT_CONTEXT.md). Pack: [CANONICAL_PRODUCT_PACK.md](CANONICAL_PRODUCT_PACK.md). External OSS routing: [EXTERNAL_REFERENCE_REPOS.md](EXTERNAL_REFERENCE_REPOS.md). UX/shell routing: [EXTERNAL_UX_REFERENCES.md](EXTERNAL_UX_REFERENCES.md).

---

## Architecture decisions locked (2026-08-20 human)

| Decision | Choice |
| --- | --- |
| Evidence vault | **Supabase Storage** (Drive = import/source only) |
| Lifecycle | **Vercel Workflow** (Queues = fan-out only) |
| Product phases | **1–8** (pilot = Phase 2; complete after 8) |
| Global IA | **Home \| Pursuits \| Intelligence \| Contracts \| Data Ops** |

Full business domains / validation / pricing / contracts / RAG / table maps live in MASTER_PRODUCT_CONTEXT — not discarded.

---

## Honest position (one screen)

| Lens | Today | Required to be |
| --- | --- | --- |
| **What L&P can actually use** | App shell, login, intake/verify **code**, empty corpus | Verified historical packages answering new RFPs with citations |
| **Phase 1 Foundation** | **Local exit gate proven** (RLS 48/48, intake, verification, processor pytest, lint/typecheck/build). Prod env still ops | Same proven in production use |
| **Phase 2 Historical Pilot** | VERIFY 2B + 2C PASS. 7 A/B source-to-canonical; schema accepted | Corpus count ~20–30 still open |
| **Phase 3 Historical Ingestion** | **PASS** — Data Ops productionized ([PHASE3_ACCEPTANCE.md](PHASE3_ACCEPTANCE.md)); DOCX wired; OCR key-gated; package grouping; resizable verify | Set `MISTRAL_API_KEY` for live scans; grow corpus |
| **Phase 4 Contracts / Compliance** | **Prompt 4 exit** — Portfolio/Renewals/Compliance nav; workspace tabs wired to verified tables; alert buckets; [PHASE4_ACCEPTANCE.md](PHASE4_ACCEPTANCE.md) | Validate on more real awarded instruments; grow corpus |
| **Phase 5 Intelligence** | Prompt 5 + **P9 workbench polish** — Buyers/Competitors/Market/Pricing/Win-Loss/Content/Reports productized: honesty strip + `n=`-bearing observation tiles on every view, `OBSERVED` vs `INFERENCE` labels, one shared Ask-launch builder, **new Recompete Radar** (verified dates only, L&P-held rows never mixed in), a win rate gated at 20 decided pursuits, and `/intelligence/buyers` → `/intelligence/clients`. [PHASE5_INTELLIGENCE_ACCEPTANCE.md](PHASE5_INTELLIGENCE_ACCEPTANCE.md) · [P9_INTELLIGENCE_WORKBENCH_ACCEPTANCE.md](productization/P9_INTELLIGENCE_WORKBENCH_ACCEPTANCE.md). **The committed VERIFY 5 24/24 is stale:** it is now **22/24** — two bare `contracts.insert` fixtures the trust trigger rejects, plus a stale grep P5 outdated | Fix verify5 fixtures/greps and re-earn the PASS; grow `contracts.verified_end_on` coverage so the radar can plan a calendar; fix the pricing rate-vs-total promoter mapping |
| **Phase 6 Ask / Reports / Automation** | Prompt 6 + **dual-rail Ask agent** (`test:phase6-ask` **46/46**; **the committed VERIFY 6 24/24 is stale — now 23/24** on a bare `contracts.insert` fixture the trust trigger rejects) + **P9 contextual launch** — every Intelligence view hands off to the one Ask surface with mode/purpose pre-set and a provenance-only context banner; eight report kinds exposed with cutoff/scope/sources/limitations — LOCATE (no LLM) / ASK streaming tools / REPORT SQL; INTERNAL_VERIFIED + PUBLIC research rails; ChatGPT Custom GPT Actions OpenAPI; providers = Gateway/Groq/Ollama/Google/optional OpenAI (**no Grok**); [PHASE6_ASK_REPORTS_AUTOMATION_ACCEPTANCE.md](PHASE6_ASK_REPORTS_AUTOMATION_ACCEPTANCE.md) | Corpus-thin; set `ASK_MODEL` + provider key; optional `TAVILY_*`/`BRAVE_*`; `GPT_ACTIONS_SECRET` + org-scoped Actions auth still ops |
| **Phase 7 Pricing Intelligence** | Prompt 7 + **P7 workbench polish** — pinned identifiers, per-truth banding + coverage counts, currency, source-click to verification, provenance sheet, read-only grid (no `onCellEdited`, paste off), honest `n=` ranges, explicit human final gate. [P7_PRICING_WORKBENCH_ACCEPTANCE.md](productization/P7_PRICING_WORKBENCH_ACCEPTANCE.md). **The committed VERIFY 7 29/29 is stale:** at clean `HEAD` it is **19/22** — its fixtures insert `awarded_rate` with no source fact (trust trigger `259fd9f`) and one check still greps `user.id` for `decided_by` | Fix verify7 fixtures/greps and re-earn the PASS; grow verified pricing corpus |
| **Phase 8 Response / Submission / Result** | Prompt 8 + **P8 submission/outcome/handoff polish** — computed submission readiness (six statuses, `UNKNOWN` never reads ready), a hard-gated **human-authorized** `markSubmissionSubmitted` with `submitted_by` attribution, honest outputs (no DOCX claim, no Google Docs integration claim), six outcome states with buyer-documented vs internal fields separated, award evidence for handoff, and a contract gate that needs a HUMAN_VERIFIED award-shaped fact **and** a human-recorded WON. [P8_SUBMISSION_RESULT_HANDOFF_ACCEPTANCE.md](productization/P8_SUBMISSION_RESULT_HANDOFF_ACCEPTANCE.md). **VERIFY 8 re-earned honestly: 23/23** after its bare `contracts.insert` fixture was replaced with the verified-award-fact path. [VERIFY8_ACCEPTANCE.md](pilot/VERIFY8_ACCEPTANCE.md) | Grow verified corpus; exercise a full mark-submitted → WON → contract chain on a real awarded package |
| **Docs** | Pack + full master context reconciled | Same |
| **App IA** | Prompt 0B remapped shell to UX_UI | Remaining: unlisted registry routes, empty corpus |
| **Production** | Often no org / sign-in | Linked Supabase + migrations + authenticated org |

**Bottom line:** VERIFY 9 fix pass = **READY WITH NONBLOCKING LIMITATIONS**. Engineering/trust gates green; deferred: grow corpus to ~20–30, `vercel login` + prod processor, `ASK_MODEL`. See [VERIFY9_ACCEPTANCE.md](pilot/VERIFY9_ACCEPTANCE.md).

---

## Next ordered work

**P0 — Production truth**  
1. ~~Apply pending migrations on live~~ **DONE** through `20260821120000` (includes `20260821090000` trust triggers).  
2. ~~Confirm Vercel Supabase env~~ **DONE** — keys existed but were **empty**; filled from local + redeployed. Prod shows sign-in (not “env not configured”). Still missing: `PROCESSOR_*`, `ASK_MODEL`.  
3. ~~Prove two-user RLS~~ **DONE** — `npm run test:phase2-rls` **51/51 PASS** on live.  
4. **Grow pilot corpus** toward ~20–30. **Progress:** **21** packages / A/B harness **21** (added PKG-20 HHSC FY26 RG06 ESBD award SRC-27 `$3,497,000`; PKG-21 HHSC R7 ESBD award SRC-28 `$617,400` via TXMAS-24-99003). **Lower bound ~20 met**; still short of ~30. SRC-19 OCR deferred. Rejected this hunt: Allen Jul-31 agenda (no L&P name), Wylie Collin finalsiste/BoardBook (no L&P), Jefferson IFB 404, extra TxDMV POs (none), duplicate Williamson minutes.  

**P1 — Trust parity**  
5–8. ~~DONE~~  

**P2 — Operator capability (when needed)**  
9. Ask dual-rail agent: in-app `POST /api/ask/chat` + `AskChatClient`; Custom GPT Actions at `/api/ask/actions/*` + OpenAPI. Local **Ollama** / Groq / Google / Gateway / optional OpenAI (**no Grok**). Public rail needs `TAVILY_API_KEY` or `BRAVE_SEARCH_API_KEY`. Gateway needs card; OpenAI quota often blocked. **Prod** still needs `ASK_MODEL` + provider + optional `GPT_ACTIONS_SECRET` / `NEXT_PUBLIC_APP_URL`.  
10. Hosted/reachable processor (`PROCESSOR_URL` + secret) beyond local.  
11. OCR / Drive tokens only when those paths are in use (`MISTRAL_API_KEY` for SRC-19).  

**P3 — Hygiene**  
12. Keep foundation audit honest; no CRM / no free-form chatbot expansion.  
13. Operator workbench VERIFY on harness-stamped facts when ready for production truth.  

---

## Session log

| When | What happened | Outcome | Follow-up |
| --- | --- | --- | --- |
| 2026-08-21 | **P10 Contract Portfolio + Renewal/Rebid Command Center, and the P1–P10 final audit** | Turned `/contracts` from a flat list into an action surface, on top of the **unchanged** Phase 9 `contracts` / `contract_alerts` / `contract_options` / `renewals` schema and the `refresh-contract-alerts` cron. **No migration, no schema change, no new dependency.** New pure module `lib/contracts/portfolio-model.ts` (no React, no Supabase, so the acceptance script bundles and runs the shipped code) is now the single source for status, buckets, values, next action and risk, so the portfolio list, the renewal queue and the Overview tab cannot disagree — `deriveContractStatus` moved here out of `load-workspace.ts` and is re-exported so no caller broke. Four rules enforced in code: **a date is verified or absent** (9 of 12 live contracts are `UNDATED`, never assumed active); **a value is a named instrument or a dash** (`NTE_CEILING` vs `PO_OBLIGATED` are never added, and `totalActiveContractValue` withholds unless coverage is complete — it printed *"only 0 of 1 active contracts record an award NTE ceiling"* on the live org rather than a number); **this is not the market radar** (`LP_PORTFOLIO_VS_MARKET_RADAR_NOTE` on portfolio, queue and renewal tab, with `/intelligence/market` linking back to `L&P-held renewals`); **nothing here acts** (`NO_AUTO_ACTION_NOTE` wherever an action is offered). Portfolio: five `n=`-bearing KPI tiles, a Buyer/Contract#/Status/Original/Current/Start/Expiration/Options/NextAction/Risk table where an absent value is an em dash and never `$0`, and five lanes that **partition** the rows (`1 + 0 + 0 + 2 + 9 = 12` live). Tabs: Overview gained award/pursuit lineage + source evidence; Service Plan kept its structure and gained per-row source links; Commercial Terms labels the four truths and adds `COMMERCIAL_PRECEDENCE` as a **reading order, not an automatic overwrite**; Changes became one append-only `Original → Amendment → Modification → Option → Renewal` timeline with undated instruments last and per-entry source links; Renewal gained bucket strip, advisory compliance readiness (empty list = **unknown, not clear**) and the rebid block. CTA renamed **Start Rebid Pursuit**; `cloneRebidFromContract` survives, links `rebid_from_contract_id`/`rebid_from_opportunity_id`, and **copies no pricing** — grep-asserted against the action source. `AutomationAuditStrip` states the job, the `15 6 * * *` schedule, `max(contract_alerts.computed_on)`, whether *this* view refreshed, and that the cron writes no term, exercises no option, sends no notice and approves nothing. **P1 respected, not reverted:** `CONTRACTS_TABS` is still `Portfolio` only; discoverability is restored by in-area cross-links, and the stale `phase4-contracts` `[ui] ContractsNav` grep was rewritten to assert that demotion **positively** (44/46 → **47/48**). `test:p10-contracts-renewal` **29/29** (new), `phase9-contracts` **6/6**, lint/tsc/build **PASS**. Wrote [P10_CONTRACT_RENEWAL_REBID_ACCEPTANCE.md](productization/P10_CONTRACT_RENEWAL_REBID_ACCEPTANCE.md) and [PRODUCTIZATION_P1_P10_FINAL_AUDIT.md](productization/PRODUCTIZATION_P1_P10_FINAL_AUDIT.md). | **Two real defects found by the browser, not by the build.** (1) `/contracts/[contractId]` 500'd — `Could not embed because more than one relationship was found for 'contracts' and 'opportunities'`, because the new Overview lineage embed collided with `opportunities.rebid_from_contract_id`; fixed by hinting `opportunities!contracts_opportunity_same_org_fkey(...)`. **tsc and build both passed with this bug present.** (2) `portfolioFilterFromParam` was case-sensitive, so a hand-typed `?filter=expiring` silently fell back to `ALL`; it now normalises case/whitespace/hyphens while a near-miss like `expiring soon` still falls back, and the acceptance check was updated to assert normalisation. IronBee proved all six contracts routes at 200 with **zero writes**: five tiles with `n=`, the withheld value statement, the five lanes summing to 12, every value cell an em dash, `Expired 2` with all other buckets `0`, all five tabs of Allen ISD `ea615b3b`, and the radar contrast from both directions. **Start Rebid Pursuit was deliberately not clicked** — it creates a permanent `opportunities` row in the operator org, which is not a clean reversible browser step; its no-pricing guarantee is grep-asserted instead. **Nine open regression failures, all reproduced identically on a clean `HEAD` worktree with P10 stashed** — seven are stale fixtures doing bare inserts that the Phase 9 trust triggers correctly reject (the opaque `Cannot read properties of null (reading 'id')` fatals in `verify2c` / `phase7-pricing` / `verify7` are that same null insert being dereferenced), two are stale UI greps against surfaces P5/P7 recomposed. **Re-earning VERIFY 5 / 6 / 7 is its own task and is a prerequisite for any release claim** — fix by sourcing fixtures through verified promotion, never by weakening a `*_require_verified_fact` trigger. **Honest limit: the command center is correct and nearly empty.** Every bucket except `EXPIRED` is zero and Active Contract Value has only ever rendered withheld, because 9 of 12 contracts carry no `verified_end_on` and none carries an NTE or a PO — the 180/120/90/60/30 queue is proven by script only. Options-remaining cannot be built (exercised status is not recorded) and amendments carry no amount; both are schema gaps to raise from pilot evidence, not to infer. CatalogIT **MIT** — renewal-queue pattern adopted, notification dispatch **declined** ([catalogit.md](reference-repos/catalogit.md)); Public-Sector CLM **no license** — reference-only, lifecycle vocabulary adopted, everything before *Active* **declined** ([public-sector-clm.md](reference-repos/public-sector-clm.md)); Whereas **GPL-3.0** (registry said AGPL — corrected) — append-only timeline adopted, clause extraction / playbook / e-signature **declined** ([whereas.md](reference-repos/whereas.md)). **Not a CLM. Not production-ready. Commercialization not started.** Parent to verify & commit. |
| 2026-08-21 | **P9 Full Intelligence Workbench polish** | Productized the seven Intelligence secondary views on top of the **unchanged** Phase 5 loaders and Phase 6 Ask/Reports engines. **No migration, no new dependency, no second chatbot, no second research engine.** Three new pure modules (no React, no Supabase, so the acceptance script bundles and runs the shipped code): `lib/intelligence/ask-launch.ts` (`ASK_LAUNCH_PATH` as the only Ask path; `buildAskHref` emitting `mode`→`purpose`→`q`→`opportunity`→`report`→`from`→`context` in fixed order; an unknown purpose falling back to `defaultPurposeForMode` so a chip can never widen retrieval; `ASK_CHIP_PURPOSE` fixing each view's purpose — competitors=`COMPETITOR_ANALYSIS`, pricing=`PRICING_ANALYSIS`, win-loss=`LOSS_ANALYSIS`, content=`LOCATE`; `serializeAskContext`/`parseAskContext`; `askLaunchViewFromParam` using **`Object.hasOwn`** so `toString`/`constructor`/`__proto__` are not views), `lib/intelligence/observations.ts` (`HONESTY_STRIP_TEXT`, `observationTile` that **cannot be constructed without its own `n=`**, `OBSERVED` vs `INFERENCE` basis, `MIN_WIN_RATE_SAMPLE = 20` with `summarizeWinLoss` + 95% Wilson interval, `observedSpan` with no median/typical), and `lib/intelligence/recompete-radar.ts`. New `components/intelligence/honesty-strip.tsx` (`IntelligenceHonestyStrip`, `ObservationTiles`, `EvidenceBasisBadge`, `AskAboutThis`). **Recompete Radar** on Market: buyer · incumbent-if-named-with-its-basis · contract · expiration/options · expected rebid · source · data status, built only from `awards`/`contracts`/`contract_options`/`renewals`/`opportunities`/`clients`/`win_loss_reviews`. **Expected rebid restates `contracts.verified_end_on` or the earliest option/notice date and names the field — never inferred from a term length, a typical cycle, or an award date**; absent both, the row says the timing is unknown. **L&P-held rows never enter the market list** (a `contracts` row in this tenant, a `WON` review, or `L_AND_P_NAME_RE` in a buyer's winner field ⇒ `L_AND_P`), and the view labels `Market radar — recompetes observed in the corpus` beside a separate `L&P-held renewals` → `/contracts/renewals` link. Filters cover only `opportunities.service_type` / `site_location` / an expected-rebid window, offer only values the corpus has, and **drop rows with unknown timing rather than assuming one**. **Win rate is gated:** below 20 decided pursuits the percentage is withheld with the reason and the current decided count; `NO_BID`/`CANCELLED`/`NO_AWARD`/`PENDING` are excluded from both sides; the rate is computed over the corpus, never the filtered table. Pricing keeps the four truths in four columns with per-truth source lineage and hands live bids back to Pursuit → Pricing. Competitors label every value `OBSERVED`/`INFERENCE` with a source cell that opens `/ingestion/verification/<documentId>`. Buyers gained deep links to solicitations/awards/contracts/win-loss plus a buyer-brief chip, still **Not CRM**. Content keeps all four reuse states and restates the drafting gates. Reports expose all **eight** kinds with cutoff/scope/sources/limitations. Ask honors incoming `mode`/`purpose`/`q` and renders a context banner stating **context is provenance only and did not narrow retrieval**; the dual-rail agent was not rebuilt. `/intelligence/buyers` is now a `permanentRedirect` to `/intelligence/clients`, with `lib/data-model/registry.ts` and `components/home/market-snapshot.tsx` corrected. `test:p9-intelligence-workbench` **40/40** (new), `test:phase6-ask` **46/46**, `test:phase10-win-loss` **13/13**, `test:phase11-hybrid-rag` **10/10**, lint/tsc/build **PASS** (all nine Intelligence routes Partial Prerender). IronBee on the live operator org proved all nine routes at 200 with no console errors, the `/intelligence/buyers` redirect in the browser, the honesty strip on all seven views with **zero** market-share language, Market's eight tiles with `Market recompetes observed` tagged `INFERENCE` and radar status `verified 0 · partial 4 · unknown 0`, a service+geo+window filter honestly cutting `4 → 0 of 4 rows`, **`Win rate withheld — sample too thin … Decided so far: 0`** with no percentage on the page, the Ask banner carrying `bids=21 · scores=8` from Competitors with `purpose=COMPETITOR_ANALYSIS` preserved as hidden inputs, `from=toString` rendering no banner and not crashing, and `purpose=NOT_A_PURPOSE` falling back to `LOCATE`. **Zero writes.** [P9_INTELLIGENCE_WORKBENCH_ACCEPTANCE.md](productization/P9_INTELLIGENCE_WORKBENCH_ACCEPTANCE.md) | Parent to verify & commit. **Pre-existing, not a P9 regression — same fixture-vs-trust-trigger drift already recorded for verify7/phase7/phase4:** `test:verify6` 23/24 fails with the literal message *"contracts.source_fact_id is required (create via verified promotion, not blank insert)"*; `test:verify5` 22/24 fails the same way at `verify5-intelligence-acceptance.mjs:299`/`:309` (two bare `contracts.insert` calls ⇒ `contracts=0`) plus a stale grep against `opportunities/[opportunityId]/page.tsx` that P5 recomposed; `test:phase5-intelligence` 24/25 on the fixture chunk pointing at a non-`HUMAN_VERIFIED` fact. **Both committed `verify5-results.json` and `verify6-results.json` said PASS and were rewritten to FAIL by their own scripts — the regenerated files are the honest state and were kept.** Re-earning VERIFY 5 / VERIFY 6 is its own task and must source fixture contracts through verified promotion, never by weakening `contracts_require_verified_fact`. **Corpus limits, not code limits:** all four market radar rows are `PARTIAL` on an award-document source alone (**incumbent not named and `verified_end_on` absent on every row**, so every `expected rebid` reads `unknown`) so the radar cannot yet plan a pursuit calendar; the win-rate branch that *shows* a rate has only ever been proven by script; and `observedSpan` reads `6.00 – 4,722,008.00 (n=31)` because `pricing_lines` mixes hourly rates with annual totals in one column — reported honestly with `n` and no median derived, but **the promoter's rate-versus-total mapping needs fixing**. Rival **MIT** — competitor density + per-entity brief launch adopted as a pattern, its Tabstack scan/Deep-Dive rail **rejected** ([rival.md](reference-repos/rival.md)); Morphic **Apache-2.0** — single-surface handoff + scope disclosure adopted, second chat surface and provider abstraction **rejected** ([morphic.md](reference-repos/morphic.md)); Tremor **Apache-2.0** — **declined a second time, nothing installed**; our tile carries a sample size where a Tremor KPI card carries a delta ([tremor.md](reference-ux/tremor.md)). **Independently re-verified and committed 2026-08-21:** `test:p9-intelligence-workbench` **40/40**, `test:phase6-ask` **46/46**, `test:phase10-win-loss` **13/13**, `test:phase11-hybrid-rag` **10/10**, lint/tsc/build **PASS** (all nine Intelligence routes Partial Prerender, `/intelligence/buyers` included). Greps re-confirmed no Intelligence view imports a chat client or a research provider (`tavily`/`exa.ai`/`serper`/`perplexity`) and that `AskChatClient` is mounted only by the Ask page itself; no market-share KPI exists anywhere — only negations of one. **The three regenerated FAIL artifacts were reproduced on a clean `HEAD` (`3b32b25`) worktree, identically:** `test:verify5` **22/24** (same two checks), `test:verify6` **23/24** (same check), `test:phase5-intelligence` 24/25 — so the committed `verify5-results.json` / `verify6-results.json` FAIL verdicts are the honest current state, not a P9 regression, and replace a stale PASS. IronBee re-proved `/intelligence/buyers` → `/intelligence/clients`, eight Market tiles all carrying `n=` with **`Market recompetes observed` the only `INFERENCE` badge**, radar `4 of 4 rows · verified 0 · partial 4 · unknown 0` labelled apart from the `/contracts/renewals` link, `Win rate withheld — sample too thin … Decided so far: 0` with **no percentage anywhere on the page**, an Ask context banner reading *"Context is shown for provenance only — it did not narrow retrieval"* with `from`/`context` preserved as hidden inputs, `purpose=NOT_A_PURPOSE` falling back to the mode default (`GENERAL_QA` on `mode=ask`, `LOCATE` on `mode=locate`), and `from=toString` rendering no banner without crashing — **zero console errors and zero React key warnings across all seven views plus Ask, and zero writes.** **One doc correction made during this verification:** the earlier claim that the radar's incumbents "are named" was wrong — the Incumbent cell is `—` on all four rows and each prints `missing: incumbent, verified expiration, options`; `PARTIAL` is earned by the award-document source alone. |
| 2026-08-21 | **P8 Submission + Outcome + Contract Handoff polish** | Made the pre-award chain `readiness → outputs → mark submitted (human) → outcome → Create Contract (verified only)` continuous, attributed and honest. New pure module `lib/opportunity/submission-readiness.ts` (six statuses where only `COMPLETE`/`NOT_APPLICABLE` are settled — **`UNKNOWN` is outstanding**, so an unmeasured packet can never read ready; `computeSubmissionReadiness` merges checklist + enabled approval layers + latest pricing decision + `computeResponseProgress` + packet logistics; `evaluateMarkSubmittedGate`; `describeSubmissionOutputs`; `evaluateContractHandoffGate`; `isAwardishFact`) with **no** React/Supabase imports. Submission workbench: sticky readiness strip (overall badge, `n/m required (p%)`, all six counts, a named `Blocking submission (n)` list), checklist status badges, a **read-only** approvals mirror pointing at Response, pricing/response mirror cards, and an Outputs section that states what each file actually is — *Word-compatible HTML (.doc), not a native DOCX/OOXML file and no DOCX writer exists in this codebase*, *there is no Google Docs integration*, exports **disabled** when the drafts are empty. **Mark SUBMITTED is now its own human-only action:** `saveSubmissionPacket` can no longer write `submitted_at` or change the stage at all; new `markSubmissionSubmitted` re-loads the checklist/layers server-side, recomputes readiness, requires the authorization checkbox, and sets `submitted_by` from the session — never a form value. New additive migration `20260821200000` adds `submitted_by` + `submission_url` + `submission_instructions` and the `submission_packets_submitted_requires_actor` check (`NOT VALID` for pre-P8 rows, enforced on every new write). Result panel: six outcome states with meanings, **buyer-documented vs internal-only** field groups, an award-evidence list tagging `Award-shaped` vs `Other` HUMAN_VERIFIED facts, and a contract CTA that shows the gate message before the click. `createContractFromWin` is idempotent, cites the verified fact, links an `awards` row with **notice only** (no amount/winner/rank/date), and **no longer infers `WON`** — the outcome must already be recorded by a human, because a verified award fact can name a competitor (Arlington's award went to Securitas at 90.46 vs L&P 82.39). **P0 fixed:** both older acceptance scripts stopped doing a bare `contracts.insert` and now assert the unsourced insert is *refused* before inserting via the verified fact — `test:verify8` back to **23/23 verdict PASS** on evidence, not by weakening a gate. `test:p8-submission-result` **44/44** (new), `test:phase8-response` **30/30**, lint/tsc/build **PASS**. IronBee on PKG-03 Arlington proved the no-checklist `UNKNOWN` state with the authorization checkbox *and* button `disabled` in the DOM, the seeded-checklist state (`0/11`, `Missing 9 · Needs signature 2 · Needs approval 1`), the honest output copy, the award-evidence split (`4 award-shaped / 19 verified`), and the contract CTA disabled with *"a verified award fact can name any bidder, including a competitor"*. The one write (11 seeded checklist rows) was deleted afterwards — pursuit re-verified at 0 checklist / 0 packet / 0 outcome / 0 contract. [P8_SUBMISSION_RESULT_HANDOFF_ACCEPTANCE.md](productization/P8_SUBMISSION_RESULT_HANDOFF_ACCEPTANCE.md) | Parent to verify & commit. **Pre-existing, not a P8 regression:** `test:phase4-contracts` 44/46 — (a) `[ui] ContractsNav` greps `section-tabs.tsx` for a symbol the shell no longer defines (stale grep; all four contract routes exist and pass), (b) `[commercial] award NTE` inserts an `awards` row with no `source_fact_id`, which the `20260821120000` trust trigger correctly rejects — same fixture drift already recorded for verify7/phase7. **Marking submitted is a record, not a submission:** nothing uploads, emails or delivers, `submitted_at` is operator-supplied and may be back-dated, and the actor constraint is `NOT VALID` so pre-P8 anonymous timestamps are not retro-attributed. `AWARDISH_FACT_RE` is keyword matching — it gates *which fact may be cited*, while the human WON outcome and `contracts_require_verified_fact` remain the real gates. Documenso **AGPL-3.0** — explicit-transition / required-action-checklist / post-complete-freeze UX adapted from public docs only, e-sign and recipient portal declined ([documenso.md](reference-ux/documenso.md)); Wraft ([wraft.md](reference-repos/wraft.md)) and RFPilot ([rfpilot.md](reference-repos/rfpilot.md)) reviewed, nothing adopted. |
| 2026-08-21 | **P7 Pricing Workbench polish** | Turned the Pursuit → Pricing Glide matrix into a dense read-first workbench without giving it a write path. New pure module `lib/opportunity/pricing-grid-model.ts` (the five truths with `provenance` + `editPath` + `gridEditable: false`, `truthCoverage`, currency/quantity formatting, shared `parseRateInput`, `observeLineGrains`, `rangeBarModel`/`sampleCountLabel`/`recencyLabel`, and HSL→RGB canvas colour maths) with **no** React/Supabase imports. Grid: `freezeColumns` on `labor_category · site_or_post · unit`, Glide **group headers** per truth, per-truth colour banding, `$67.34` currency with `—` for absent, source cells navigating to `/ingestion/verification/<documentId>`, rate cells opening a provenance Sheet (rate · provenance · how-this-changes · **Editable in grid: No** · source fact · jump to cost model for internal cost), `rowMarkers="both"` + multi-row/multi-rect selection + `copyHeaders`, hover theming, and shadcn tokens resolved to concrete RGB with a `MutationObserver` for light/dark. Comparables became a ten-column dense table (source pursuit + buyer, why-comparable, grain, three rates, **which truths are sourced**, **recency**) with range cards that state `n=…` always and draw a bar only at **n ≥ 3**; whitespace-only reasons are now rejected in-browser instead of throwing server-side. Final bid reads **FINAL PRICE — HUMAN DECISION REQUIRED** + `No AI approval path`. Buyer format is read off real line grains with an explicit *supported-but-not-observed* list. Overview snapshot now derives from the same five-truth model and currency formatter. **No schema change, no migration, no new dependency (Tremor declined — the range bar is four divs), and no new writer to `pricing_lines`: the grid declares no `onCellEdited` and `onPaste={false}` because a grid edit would create a rate with no `source_fact_id`.** `test:p7-pricing-workbench` **35/35**, `test:phase7-four-truth` **10/10**, lint/tsc/build **PASS**. `test:phase7-pricing` 12/13 and `test:verify7` 19/22 — **all failures reproduced identically on a clean `HEAD` worktree** (see follow-up). IronBee browser on PKG-03 Arlington proved all five columns render with `Buyer awarded 8/8` and four truths at `0/8`, pinned identifiers, banding, `$` rates, the provenance sheet, source-click → verification, `2 row(s) selected`, `n=0` honest-empty ranges, the blank-reason rejection, the invalid-rate block on the cost model, and the observed buyer format — **with zero writes** and zero console errors on reload. [P7_PRICING_WORKBENCH_ACCEPTANCE.md](productization/P7_PRICING_WORKBENCH_ACCEPTANCE.md) | Independently re-verified and committed 2026-08-21: `test:p7-pricing-workbench` **35/35**, `test:phase7-four-truth` **10/10**, lint/tsc/build **PASS** (Pricing route still Partial Prerender); IronBee on PKG-03 Arlington re-proved the five columns at `Buyer awarded 8/8` with four truths at `0/8`, frozen identifiers surviving a horizontal scroll, the `$67.34` provenance Sheet (`Editable in grid: No`, fact `0dec3c9e…`), a Source cell navigating to `/ingestion/verification/3ad07085…`, the whitespace-reason rejection leaving comparables at **0 included / 26 excluded**, and `31.555` blocking the cost-model submit at `No unsaved changes` — **zero writes**. **The committed VERIFY 7 “29/29 PASS” is stale — not a P7 regression.** At clean `HEAD` (`c3eeb8f`) `test:verify7` is **19/22** and `test:phase7-pricing` **12/13**, identically: (a) both suites insert `pricing_lines.awarded_rate` with no `awarded_source_fact_id`, which the trust trigger from `259fd9f` (`20260821120000`) correctly rejects — the insert returns null and the fixture dereferences `.id`; (b) verify7 greps `actions.ts` for `decided_by: approve ? user.id` but that became `userId` in `c1747dd`. **Fix those fixtures/greps and re-earn VERIFY 7 as its own task.** `docs/pilot/VERIFY7_ACCEPTANCE.md` + `docs/benchmarks/verify7-results.json` were restored to `HEAD` rather than carried here. Glide Data Grid **MIT** (installed 6.0.3) — display/selection/theming APIs adopted, **editing API rejected** ([glide-data-grid.md](reference-repos/glide-data-grid.md)). Range bars proven only by script (Arlington has 0 included comparables); `site_or_post` on that pursuit is page markers, a corpus-quality finding. **Second corpus-quality finding from this verification:** Arlington's eight `pricing_lines.labor_category` values are bidder names (`Security Engineers, Inc.,`, `Blackstone Security`, `Bid closed`, …), not labor categories, so the workbench honestly renders eight "labor categories" and eight cost-model editors that are really eight bidders, and `unit` is null on all eight so `hourly` is correctly *not* an observed structure. The workbench displays the grain it was promoted; **the promoter/extraction mapping for award-summary tables is what needs fixing.** |
| 2026-08-21 | **P6 Requirement-Driven Response Workspace** | Turned the Response tab into a proposal-authoring surface where what the operator may do follows the evidence state of the selected requirement. New pure module `lib/opportunity/response-workspace-model.ts` (work-state buckets, seven left-nav filters with counts, `evaluateDraftGate`, `buildResponseSavePayload`, approved-share progress) with **no** Supabase/React imports; new `components/opportunity-workspace/response-source-sheet.tsx` (`ui/sheet`, saved `sources_used` + retrieved passages, each linking to `/ingestion/verification/<documentId>`); `response-tiptap-editor.tsx` adapted with a `@tiptap/react/menus` bubble menu, a seven-item slash stub, 1500 ms debounced autosave, Ctrl/Cmd+S, and a visible unsaved/saving/saved indicator; requirements matrix rows open a detail Sheet that deep-links to `…/response?req=<id>`. **Phase 8 business logic was not rebuilt, no schema change, no new dependency, no new pursuit tab.** Trust: Generate is disabled on `L_AND_P_INPUT_REQUIRED` (Request L&P input stays enabled) with an explicit override that keeps the warning; `REVIEW_REQUIRED` requires source selection; a `DO_NOT_USE`/`SUPERSEDED`/unretrieved chunk is rejected **above** the override so nothing can bypass it; an approved response is locked against regeneration until reopened; `approve=1` is emitted only by the `APPROVE` intent, so autosave can never approve. Retrieval now follows the selection (`loadRequirementEvidence`) instead of describing `requirements[0]`. `test:p6-response-workspace` **41/41**, lint/tsc/build **PASS**, `test:phase8-response` 24/25 and `test:verify8` 21/23 (all failures pre-existing on the `contracts.source_fact_id` trigger from `71062e7`; every response check passed). IronBee browser on PKG-03 Arlington proved filters, selection-driven retrieval, the autosave indicator saving a **DRAFT**, the L&P gate on/override/off, live progress recount, the source sheet, and the matrix deep link; the one test write was reverted (0 responses, 12 `OPEN`). [P6_RESPONSE_WORKSPACE_ACCEPTANCE.md](productization/P6_RESPONSE_WORKSPACE_ACCEPTANCE.md) | Parent to verify & commit |
| 2026-08-21 | **P5 Pursuit Overview + Bid Strategy** | Rewrote the pursuit Overview from a metadata form into eleven dense read-first sections that answer what this is, when it is due, what is required, how it is scored, bid or not, who the buyer is, who else is bidding, what L&P has done before, what is risky, whether we are compliance-ready, what the evidence supports, and what to do next. New pure module `lib/opportunity/overview-model.ts` (requirement rollup, evaluation weight audit, score reading, compliance buckets, `buildBidStrategy`, `buildNextActions`) with **no** Supabase/React imports; new `lib/opportunity/load-overview-bundle.ts` composing 20 parallel RLS reads over existing loaders; new `components/opportunity-workspace/overview-sections.tsx`. **No schema change, no new pursuit tab, no LLM call, no LangGraph.** Every bullet carries an openable citation; absent evidence produces a named `withheld` line; zero bullets ⇒ `INSUFFICIENT` with canonical copy. No score, rate, share, probability, or causal claim is generated — asserted by grep in the new test. Bid/no-bid stays human. `test:p5-pursuit-strategy` **26/26**, `test:phase5-verification` **3/3**, `test:phase6-ask` **46/46**, lint/tsc/build **PASS**. IronBee verified on **PKG-03 Arlington** `b937b54c…` (scores recorded but criteria absent, and an `awards` row with a null winner — the incumbent stays unknown, not inferred) and empty **PKG-05 Jefferson County** `2ea6f404…` (all honest empty states, `INSUFFICIENT` strategy). Metadata-form save round-trip exercised then reverted; zero console errors. `PursuitIntelligenceSummary` / `ProposalPacketGaps` unmounted here but **not deleted**. [P5_PURSUIT_STRATEGY_ACCEPTANCE.md](productization/P5_PURSUIT_STRATEGY_ACCEPTANCE.md) | Independently re-verified and committed 2026-08-21. **Pre-existing, not a P5 regression:** `test:phase5-intelligence` `[schema] reuse_status accepts REVIEW_REQUIRED` fails identically (24/25) on a clean `HEAD` git worktree — its fixture chunk points at a non-`HUMAN_VERIFIED` fact. Its `[ui] Pursuit Overview surfaces intelligence summary` check was updated to follow the new composition. `generateIntelligenceReport("bid_strategy")` deliberately **not** wired in — it would have written narrative over thin rows. Compliance readiness stays honestly Unknown pre-award until a pursuit-level compliance store exists. RFPilot **MIT** — taxonomy adopted, its ungrounded `match_score` / `win_strategy_tips` generation **rejected** ([rfpilot.md](reference-repos/rfpilot.md)); Open Deep Research **MIT** — citation discipline adopted, LangGraph orchestration **declined** ([open-deep-research.md](reference-repos/open-deep-research.md)); OpenContracts verify/source↔fact pattern reused ([opencontracts.md](reference-repos/opencontracts.md)). |
| 2026-08-21 | **P4 independent verification + commit** | Re-audited P4 from a clean shell: `test:p4-discovery` **8/8**, `test:p4-discovery-rls` **14/14**, lint/tsc/build **PASS** (discover/watchlist/submitted/closed all Partial Prerender). Browser (IronBee): Discover shows 5 labeled fixtures + amber SAMPLE DATA banner; **`public_sources` measured at 0 rows after repeated views** (nothing persisted on view); Watch → exactly 1 row (`fixture`, `estimated_value` null) → Watchlist shows provider `sample`; Active/Submitted/Closed render their own stage slices; zero console errors from the app origin; fixture row deleted afterward (`public_sources` 0, provider-tagged `research_facts` 0, public-provenance pursuits 0). Confirmed `AI_EXTRACTED`-only writes in code **and** the live DB constraint `research_facts_verified_requires_actor`. Corrected an inaccurate reference note: `rfp-map` **is** identifiable — [EthanHNguyen/rfp-map](https://github.com/EthanHNguyen/rfp-map), **MIT** — but its map/mobile-first UX was **declined** for our dense desktop discovery table; registered as entry 19 and recorded as pattern-declined, not unidentifiable. No product code changed. | Committed and pushed to `main`. **Live SAM.gov path still never exercised against a real response** — set `SAM_GOV_API_KEY` and reconcile the first keyed run against the actual payload. Fixture notices count toward nothing in the Historical Pilot. |
| 2026-08-21 | **P4 Public Opportunity Discovery + Watchlist + Start Pursuit** | New `public_sources` table + pursuit provenance columns (migration `20260821180000`, **applied** to remote). Provider layer `lib/procurement/providers/` — SAM.gov live when `SAM_GOV_API_KEY` set, otherwise clearly labeled `FIXTURE-SAM-*` sample data with an amber honesty banner; thin manual adapter. `PURSUITS_TABS` = Discover \| Watchlist \| Active \| Submitted \| Closed, all wired with `PursuitsNav`; `PursuitsList` parameterized by stage view + Origin column. Discover persists **nothing** on view; rows land only on Watch / Dismiss / Start pursuit. Start pursuit → `INTAKE` / `PENDING` + provenance + one **`AI_EXTRACTED`** `research_facts` row (guarded in code and by DB constraint). No AI fit score. Buyer linked only when a `clients` row already matches — no client invented from a listing. `test:p4-discovery` **8/8**, `test:p4-discovery-rls` **14/14**, lint/tsc/build PASS, IronBee browser verified (watch → start → workspace → dismiss), zero console errors; fixture artifacts deleted from the live org afterward. [P4_OPPORTUNITY_DISCOVERY_ACCEPTANCE.md](productization/P4_OPPORTUNITY_DISCOVERY_ACCEPTANCE.md) | Parent to verify & commit. **Live SAM.gov path never exercised against a real response** — treat first keyed run as unvalidated. TenderRadar + OpenSAM both **unlicensed** → reference-only ([tenderradar.md](reference-repos/tenderradar.md), [opensam.md](reference-repos/opensam.md)); `rfp-map` = [EthanHNguyen/rfp-map](https://github.com/EthanHNguyen/rfp-map), **MIT** and copy-eligible, but its map/mobile-first UX was **declined** for a dense desktop discovery table; bulk-CSV acquisition noted as a future option ([rfp-map.md](reference-repos/rfp-map.md)) |
| 2026-08-21 | **P3 Executive Home + Action Center** | Replaced `/overview` with owner command center: KPI strip (10 real metrics), Needs Attention queue, Pipeline table, Win/Loss snapshot, Contract alerts, Market snapshot. All RLS-scoped queries; no fake metrics; honest empty states; click-throughs verified. lint/tsc/build PASS; IronBee browser verified. [P3_EXECUTIVE_HOME_ACCEPTANCE.md](productization/P3_EXECUTIVE_HOME_ACCEPTANCE.md) | Parent to verify & commit |
| 2026-08-21 | **P2 Real Corpus Data Ops productization** | Hardened intake UX (preflight validation, per-file status, success links), processing queue (lifecycle_error, operator badges, filter chips, filename links), OCR_REQUIRED semantics, re-extract guard (skip HUMAN_VERIFIED), workbench speed (keyboard e, optimistic updates, auto-advance), exceptions accuracy (doc join, filters, disposition notes), corpus funnel report script, registry path fix. Lint/tsc/build/pytest PASS. [P2_REAL_CORPUS_DATA_OPS_ACCEPTANCE.md](productization/P2_REAL_CORPUS_DATA_OPS_ACCEPTANCE.md) | Parent to verify & commit |
| 2026-08-20 | **P1 independent verification** | Code + IronBee browser: IA gates **PASS**. Found mobile nav dead (trigger inside closed Sheet); fixed header `SidebarTrigger` `md:hidden` + trigger `aria-label`. lint/tsc re-PASS; build previously PASS 68 routes. Acceptance updated. [P1_UX_FOUNDATION_ACCEPTANCE.md](productization/P1_UX_FOUNDATION_ACCEPTANCE.md) | Optional: reduce `useIsMobile` hydration warnings; commit when requested |
| 2026-08-20 | **P1 UX Foundation** | Sidebar IA fix (Intelligence/Data Ops → single links); shell primitives (`PageHeader`, `WorkspaceHeader`, `EmptyState`, `CollectionPage`); workspace shells condensed; CONTRACTS_TABS demoted Renewals/Compliance (pages still work with breadcrumb back); collection pages wired. lint/typecheck/build **PASS**. [P1_UX_FOUNDATION_ACCEPTANCE.md](productization/P1_UX_FOUNDATION_ACCEPTANCE.md) | Parent to verify & commit |
| 2026-08-20 | External UX/shell registry (no product code) | [EXTERNAL_UX_REFERENCES.md](EXTERNAL_UX_REFERENCES.md) + `docs/reference-ux/README.md` + `.cursor/rules/external-ux-references.mdc`. 12 UX refs REGISTERED ONLY (Plane/Twenty AGPL visual; Studio Admin + shadcn for code). IA lock restated; skeleton listed not built. No shadcn install, no page redesign, nothing committed. | Explicit shell/skeleton task later if requested; do not load all UX refs per task |
| 2026-08-20 | External reference registry (no product code) | [EXTERNAL_REFERENCE_REPOS.md](EXTERNAL_REFERENCE_REPOS.md) + lazy `docs/reference-repos/README.md` + `.cursor/rules/external-reference-repos.mdc`. 18 repos REGISTERED ONLY; max-3 subsystem inspect. No clones, no schema/app changes, nothing committed. | Inspect a listed repo only when a subsystem gap needs it; write a lazy note then |
| 2026-08-20 | Ask dual-rail AI SDK upgrade | Shipped evidence model, Gateway/Groq/Ollama/Google/OpenAI (no Grok), internal+public tools, `streamAskChat`, `POST /api/ask/chat`, AskChatClient on mode=ask, Custom GPT Actions OpenAPI + routes, `.env.example` + phase6 asserts. Public never writes to `document_chunks`. LOCATE still no LLM. | Set provider keys / Gateway card / Tavily; harden Actions auth to operator org under bearer; grow corpus |
| 2026-08-20 | Ask providers + push | Multi-provider Ask (`synthesize.ts`): Ollama local free path verified; Google Gemini OK; Gateway needs card; OpenAI quota fail. Corpus harness SRC-24…30 + BoardBook extractor fix. Secrets not committed. | Grow to ~30; prod Ask env; hosted processor |
| 2026-08-20 | Verified P0 vs end-state; cross ~20 | Acquired/ingested SRC-27/28 public TxSmartBuy ESBD awards (HHSC). Live **21** pkgs / **21** A/B complete. VERIFY2B **8/8**. Lower bound of ~20–30 **met**. | Continue toward ~30 (prefer Grade A instruments); ASK_MODEL; MISTRAL; hosted processor |
| 2026-08-20 | Verified P0 vs end-state; grow corpus | Acquired/ingested SRC-24 Terrell 2023 ESR, SRC-25 TxSmartBuy TXMAS list, SRC-26 Allen minutes excerpt. Live **19** pkgs. Extractor glued-BoardBook fix + reextract SRC-24 NTE `$300000` + LP award. VERIFY2B **8/8**. Browser Contracts shows SRC-24/25. Rejected false TxDMV FTP hits. | ≥1 more package to cross ~20; Grade A instruments preferred; ASK_MODEL; MISTRAL; hosted processor |
| 2026-08-20 | Section FAIL fix-pass + next corpus | False PO root cause fixed; digit CHECK; purged garbage. VERIFY2B **8/8**. Added Wylie/Mesquite B packages → live **17** pkgs / **16** A/B complete. | More Grade A instruments; MISTRAL; ASK_MODEL |
| 2026-08-20 | Section audit FAIL → fix-pass | Root cause: `_PO` matched inside “political/positions”. Tightened extractor + digit CHECK; purged 6 bad POs + REJECT garbage/dup facts. pytest **16/16**. SRC-19 OCR deferred. [SECTION_TERRELL_AUDIT_FIX.md](pilot/SECTION_TERRELL_AUDIT_FIX.md) **PASS WITH DEFERRED** | Grow corpus ~20–30; MISTRAL for SRC-19; ASK_MODEL |
| 2026-08-20 | Verified next vs end-state; implement | Confirmed P0 = corpus (~20–30). Acquired Terrell 2026–27 + ESRs/price; discarded non-L&P Williamson funding. Extractor POP dates/NTE; Allen+Terrell `verified_end_on`; Renewals UI shows **2 EXPIRED**. Packages **15**, A/B harness **14**. | More public packages; ASK_MODEL; OCR for SRC-19; workbench VERIFY |
| 2026-08-20 | Implement next steps | Acquired Terrell BoardBook contract (May 19 agenda file 6676708); ingested SRC-20 + SRC-01b. Extractor+RPC for required_form/cost_component; re-extract SRC-09/12/14. Live: packages **14**, renewals **1** (CPI-W), forms **12**, cost stack **10**, contracts **7**. Browser: Contracts portfolio shows Terrell/Harris/Williamson. | More acquisitions to ~20–30; ASK_MODEL; hosted processor; workbench VERIFY |
| 2026-08-20 | Verified next vs end-state; fill empty mapped tables | Expanded extractor (PO/TXMAS/GSA/eval scores/competitors/sections/amendments); re-extract+promote. Live: contracts **3**, POs **4**, federal **2**, competitor lines **18**, eval scores **8**, proposal sections **4**, amendments **2**, chunks **130**, pricing_lines **24** | Acquire packages toward ~20–30; ASK_MODEL; workbench VERIFY |
| 2026-08-20 | Document→table mapping | `DOCUMENT_TABLE_MAPPING.md` + `document-table-map.ts` + Settings Data model UI; migration competitor_pricing_lines promote; packages linked; full promote chain in pilot/backfill | Grow corpus; re-extract bid tabs for competitor rows; workbench VERIFY |
| 2026-08-20 | Corpus push on this PC | Staged all USABLE PDFs → `docs/pilot/acquired/`; processor + `PILOT_SRC_IDS` ingest SRC-08 + C; **8 A/B** harness-complete; **6 C** VERIFIED (no L&P promote); **35 chunks** via backfill; VERIFY2B **8/8** | Acquire more packages toward ~20–30; workbench VERIFY; ASK_MODEL / hosted processor |
| 2026-08-20 | Lasting operator + agent login | `LP_OPERATOR_*` in `.env.local`; ensure-operator → global_admin; local auto-login verified; pilot keeps operator org `f9f6632f-…` (no delete) | Agents use LP_OPERATOR; grow corpus |
| 2026-08-20 | Honesty: harness ≠ human VERIFY | Prior “HUMAN_VERIFIED / pipeline-complete” language overstated — script stamped status; workbench (source PDF + page/excerpt) is the real human gate | Reset stamp / walk real Verification UI if operator wants |
| 2026-08-20 | Processor + A/B ingest→verify→promote | Local processor healthy; 7 A/B **harness**-stamped/promoted (not workbench-eyeballed); rates e.g. Williamson $31.45 proposed, Allen $32.28 awarded | Real human VERIFY in UI; grow packages; ASK_MODEL later |
| 2026-08-20 | Re-acquire public pilot PDFs on this PC | Manifest checksum MATCH for SRC-01/02/03/04/06/07/09/13; Tarrant extras; Arlington via browser (CDN 403 to curl) | Ingest A/B; keep hunting Jefferson/Dallas/TFC/Harris/VA |
| 2026-08-20 | Execute P0→P1 | Live migrations through trust+append-only; RLS 51/51; Vercel empty env filled + redeploy; prod sign-in OK | Grow corpus; commit when asked; processor/ASK_MODEL later |
| 2026-08-20 | Re-audit main vs original goal (no new remote commits past `aae9ff1`) | Architecture still locked; corpus/ops/trust residual gaps unchanged; Vercel CLI linked on this machine | Execute P0→P1 ordered work |
| 2026-08-20 | Schema/RLS audit follow-up | Trust migration + gate `createContractFromWin`; docs updated | Apply migration on live Supabase |
| 2026-08-19 | Pass 3+4 ops UI | `547e16c` | Migrations |
| 2026-08-19–20 | Pack sync + pack (1) accuracy | Phases 1–8; multi-tenant-ready | — |
| 2026-08-20 | User: master context over-condensed | Human locked Storage/Workflow/1–8/pack IA; **rewrote MASTER_PRODUCT_CONTEXT** with full domains + table/Python map | Commit when asked; then pilot |
| 2026-08-20 | Prompt 0B IA reconcile | Global shell Home / Pursuits / Intelligence / Contracts / Data Ops; Ask GPT in header; pursuit + contract nested tabs | Lint/typecheck/build; remaining unlisted routes |
| 2026-08-20 | User shared 15 public security PDFs (Dallas tabs, Harris renewal, TFC VSA, MHMR 25-003, Arlington TX 22-0143, TxDMV PO, Allen board, VA rider 19-264-R) | Domain evidence read only — **not ingested**. Confirms site-varying rates, pay vs bill, cost-build, OT/holiday, CPI options, rider/PO, eval scorecards | Pilot still 0 through pipeline; schema gaps stay findings |
| 2026-08-20 | VERIFY 0 docs + IA | Stale CURRENT_STATE_AUDIT IA claim removed; `/procurement/requirements` redirects to Pursuits; Ask GPT visible in header at all breakpoints | lint/typecheck/build |
| 2026-08-20 | Prompt 1 Foundation hardening | Re-ran Foundation gates; **no foundation code rebuild**. Added `npm run test:foundation` + `test:processor`. Audit: Phase 1 local exit proven | Phase 2 pilot; Vercel org/env; 1-package smoke |
| 2026-08-20 | VERIFY 1 Foundation acceptance | Independent proofs; fixed version append, actor attribution, append-only provenance, processor VERIFIED guard. Applied remote migrations including `20260820400000` | Historical Pilot |
| 2026-08-20 | Prompt 2A usable corpus | [PILOT_CORPUS_MANIFEST.md](pilot/PILOT_CORPUS_MANIFEST.md): **18 USABLE** files, **13 packages**. Frisco L&P bid tab **UNAVAILABLE** (not counted). No schema/AI work | Prompt 2B ingest only YES rows |
| 2026-08-20 | VERIFY 2A corpus fix pass | Fixed manifest counts/metadata (SRC-01/06/09); hardened `verify2a-corpus-audit.mjs`; `npm run test:verify2a` **132/132 PASS**. [VERIFY2A_ACCEPTANCE.md](pilot/VERIFY2A_ACCEPTANCE.md) | Prompt 2B ingest |
| 2026-08-20 | Prompt 2B Historical Pilot | Ran 17/18 USABLE files through intake→parse→verify; [PILOT_GAP_REPORT.md](benchmarks/PILOT_GAP_REPORT.md). **0 structured promotion.** Scripts: `phase2-pilot-run.mjs`, `phase2-pilot-gap-report.mjs` | Structured extractor + schema from gap report |
| 2026-08-20 | VERIFY 2B Historical Pilot acceptance | Independent audit: **FAIL**. Trust pipeline not completed; batch VERIFIED overcount; C corpus not promoted as L&P history. [VERIFY2B_ACCEPTANCE.md](pilot/VERIFY2B_ACCEPTANCE.md) | Do not start Phase 3 until blockers close |
| 2026-08-20 | VERIFY 2B fix pass | Structured PDF extractor; opportunity-linked promote; evidence-bound VERIFY; no zero-fact VERIFIED; precedence probe. **PASS WITH NONBLOCKING GAPS** (7 A/B complete). `test:verify2b` 8/8 | Deferred: OCR, 25MB, corpus count |
| 2026-08-20 | Prompt 2C pilot schema | Evidence-backed tables/columns only ([PHASE2C_SCHEMA.md](PHASE2C_SCHEMA.md)); promote RPC grain fix; types updated. 2c 17/17, RLS 48/48, four-truth 10/10, lint/typecheck/build PASS | STOP — no Phase 3 |
| 2026-08-20 | VERIFY 2C schema acceptance | Independent audit vs [PILOT_GAP_REPORT.md](benchmarks/PILOT_GAP_REPORT.md). **PASS WITH NONBLOCKING GAPS.** `test:verify2c` 58/58. Flags: competitor_bids vs competitor_pricing_lines overlap; required_forms fact-only provenance; no amendments table. [VERIFY2C_ACCEPTANCE.md](pilot/VERIFY2C_ACCEPTANCE.md) | STOP |
| 2026-08-20 | VERIFY 2C fix pass | Closed all gaps: `required_forms.source_document_id`; drop competitor_bids hourly/rate_type; enrich `contract_amendments` number/title; evidence-only service-plan comment. Migration `20520000`. **PASS.** `test:verify2c` 65/65 | STOP |
| 2026-08-20 | Prompt 3 Phase 3 production | DOCX parser; Mistral OCR key-gated; 50 MB intake; package grouping; Resizable+TanStack verify; VIEW SOURCE/RESOLVE audit; JobPort embed fan-out; `test:phase3-production` 18/18. [PHASE3_ACCEPTANCE.md](PHASE3_ACCEPTANCE.md) | STOP — no Phase 4 |
| 2026-08-20 | VERIFY 3 Data Ops acceptance | Real pilot PDFs (SRC-02/03/08/19/01). **PASS 26/26.** XLSX openpyxl fixture (pilot hole); DOCX not pilot-required; scans escalate without OCR key. [VERIFY3_ACCEPTANCE.md](pilot/VERIFY3_ACCEPTANCE.md) | STOP |
| 2026-08-20 | VERIFY 3 fix pass | Re-audited report: **0 FAIL assertions.** No code fix required. Deferred only: `MISTRAL_API_KEY` for live OCR. Re-ran verify3 26/26 + phase3/7/RLS/2c PASS | STOP |
| 2026-08-20 | Prompt 4 Phase 4 Contracts | ContractsNav; Overview/Service Plan/Commercial/Changes/Renewal wired to verified tables; company compliance; alert buckets. `test:phase4-contracts` **44/44**; phase9 **6/6**; lint/typecheck/build PASS; browser: Portfolio/Renewals/Compliance | STOP — no Phase 5 |
| 2026-08-20 | VERIFY 4 fix pass | Closed F1–F5 (Overview original/current/NTE; Changes option exercises; Renewal internal review + rebid status; Service Plan absent-obligation honesty). [VERIFY4_ACCEPTANCE.md](pilot/VERIFY4_ACCEPTANCE.md) **PASS**. `test:phase4-contracts` **46/46**; phase9 **6/6**; typecheck PASS | STOP |
| 2026-08-20 | VERIFY 4 lifecycle | Real instruments SRC-02/04/14/15/16. Prove original preserved, Amend 4 current truth, PO history, service plan, options, CPI-W renewal, alert buckets, rebid, compliance readiness, award link. `test:verify4` **25/25 PASS**. [VERIFY4_ACCEPTANCE.md](pilot/VERIFY4_ACCEPTANCE.md) | STOP |
| 2026-08-20 | VERIFY 4 fix pass | Closed promote gap: PO / payment_terms / TXMAS / service_plan / amendment_number via `20260820600000`. Fixed PL/pgSQL ambiguity. `test:verify4` **31/31**; phase4 **46/46**. OCR remains external deferred only | STOP |
| 2026-08-20 | Prompt 5 Phase 5 Intelligence | Buyers portfolio; Competitors bids/lines/scores; Market verified-only (no doc counts); Win/Loss lessons+scores; Content reuse REVIEW_REQUIRED; Pursuit Overview intel summary. `test:phase5-intelligence` **25/25**; phase10 **13/13**; lint/typecheck/build PASS. [PHASE5_INTELLIGENCE_ACCEPTANCE.md](PHASE5_INTELLIGENCE_ACCEPTANCE.md) | STOP — no Phase 6 |
| 2026-08-20 | VERIFY 5 Intelligence acceptance | Independent audit `test:verify5` **23/24 FAIL**. Sole gap: `competitor_pricing_lines` allows unsourced insert (no has_source check). All other Prompt VERIFY checks PASS. [VERIFY5_ACCEPTANCE.md](pilot/VERIFY5_ACCEPTANCE.md) | STOP — no fix pass unless asked |
| 2026-08-20 | VERIFY 5 fix pass | Added `competitor_pricing_lines_has_source` (`20260820710000`). `test:verify5` **24/24 PASS**; phase5-intel **25/25**; phase10 **13/13**; phase11 **10/10** | STOP |
| 2026-08-20 | Prompt 6 Phase 6 Ask/Reports/Automation | Header Find/Ask; LOCATE/ASK/REPORT; purpose-aware `search_verified_knowledge`; 8 report generators; `automation_events` + pg_cron + Vercel digest cron; DO_NOT_USE blocked for PROPOSAL_DRAFTING. `test:phase6-ask` **25/25**; phase11 **10/10**; lint/typecheck/build PASS. [PHASE6_ASK_REPORTS_AUTOMATION_ACCEPTANCE.md](PHASE6_ASK_REPORTS_AUTOMATION_ACCEPTANCE.md) | STOP — no Phase 7 |
| 2026-08-20 | Phase 6 fix pass | No VERIFY6 on disk. Closed Prompt 6 answer-contract gap: Sources/Evidence + View Source always render (empty states). `test:phase6-ask` **25/25**; phase11 **10/10**; browser insufficient path shows full contract. Deferred: `ASK_MODEL`/Gateway; finer automation awaiting Phase 8 submission fields | STOP |
| 2026-08-20 | VERIFY 6 Ask/Reports/Automation | Independent audit `test:verify6` **23/24 FAIL**. Sole gap: no `approval_reminder` automation that respects proposal/approval state. All LOCATE/ASK/purpose/tenancy/reports + other automation assertions PASS. [VERIFY6_ACCEPTANCE.md](pilot/VERIFY6_ACCEPTANCE.md) | STOP — no fix pass unless asked |
| 2026-08-20 | VERIFY 6 fix pass | Added `refresh_approval_reminder_alerts` (`20260820810000`): fires only when `go_no_go=PENDING` + pre-submit stage; auto-clears on GO/NO_GO or SUBMITTED/CLOSED/AWARDED; never auto-approves. `test:verify6` **24/24 PASS**; phase6-ask **25/25**; phase11 **10/10** | STOP |
| 2026-08-20 | Prompt 7 Phase 7 Pricing | Pursuit Glide five-truth workbench; cost model H&W/vehicles/travel/WD; comps include/exclude; `pricing_decisions` human final bid; Intelligence cross-corpus pricing. `test:phase7-pricing` **17/17**; four-truth **10/10**; lint/typecheck/build. [PHASE7_PRICING_ACCEPTANCE.md](PHASE7_PRICING_ACCEPTANCE.md) | STOP — no Phase 8 |
| 2026-08-20 | VERIFY 7 Pricing acceptance | Independent audit `test:verify7` **FAIL**. Gaps: empty `pricing_comparable_judgments.reason` allowed at DB; Glide five-truth matrix has no source fact links (comps FactRef still works). All prove-list items otherwise PASS. [VERIFY7_ACCEPTANCE.md](pilot/VERIFY7_ACCEPTANCE.md) | STOP — no fix pass unless asked |
| 2026-08-20 | VERIFY 7 fix pass | `pricing_comparable_judgments_reason_nonblank` (`20260820910000`); Glide source_fact URI columns → verification. `test:verify7` **29/29 PASS**; phase7-pricing **17/17**; four-truth **10/10** | STOP |
| 2026-08-20 | Prompt 8 Phase 8 Response/Submission/Result | Pursuit Requirements matrix; Tiptap Response workspace + evidence states + DO_NOT_USE gate; configurable approvals; submission packet/checklist/exports; result capture + contract-on-win. `test:phase8-response` **25/25**; lint/typecheck/build PASS. [PHASE8_RESPONSE_ACCEPTANCE.md](PHASE8_RESPONSE_ACCEPTANCE.md) | STOP |
| 2026-08-20 | VERIFY 8 Proposal workflow | Independent E2E audit Arlington/Lottery-shaped solicitation package. `test:verify8` **23/23 PASS** (steps 1–22). No fabricated L&P historical rates. [VERIFY8_ACCEPTANCE.md](pilot/VERIFY8_ACCEPTANCE.md) | STOP |
| 2026-08-20 | Full functional re-pass | VERIFY5 **24/24**, VERIFY6 **24/24**, VERIFY7 **29/29**, VERIFY8 **23/23**, phase8-response **25/25**. Browser: Save draft / checklist / Result UI smoke. Fixed Cache Components blocking-prerender via Suspense on all Pursuit workspace pages; Glide grid `dynamic(..., { ssr: false })` for `window is not defined`. Console clean on Overview→Result | STOP |
| 2026-08-20 | Prompt 9 release readiness | Full lifecycle audit + regression. Hardened: redirect typecheck/build; VERIFY2C allow Phase 8 `requirement_responses` (**66/66**). Wrote [RELEASE_READINESS_REPORT.md](RELEASE_READINESS_REPORT.md): **not production-ready** (corpus/ops); no trust/security/submission suite FAIL | STOP |
| 2026-08-20 | VERIFY 9 Final release | Auditor-only. Business goal vs live corpus/ops. **NOT READY.** Blockers: pilot corpus exit unmet; prod runtime unproven; ASK_MODEL absent. [VERIFY9_ACCEPTANCE.md](pilot/VERIFY9_ACCEPTANCE.md) | Grow corpus; prod proof; ASK_MODEL; re-VERIFY 9 |
| 2026-08-20 | VERIFY 9 fix pass | Closed fixable gaps (pilot 50 MB + SRC-03 unblocked; live chunk promote). Remaining three items = **deferred external**. Verdict → **READY WITH NONBLOCKING LIMITATIONS**. Evidence: verify8 23/23, verify6 24/24, RLS 51/51, phase8 25/25, verify2b 8/8 | Push when asked; acquire PDFs / vercel login / ASK_MODEL |

Older: `git log`.
