# Current state audit — canonical product maturity snapshot

**Date context:** August 2026. Prompt 0A docs + Prompt 0B shell IA + VERIFY 0 + Prompt 1 Foundation gate + **productization P1–P10 complete** (2026-08-21). Programme-level view: [PRODUCTIZATION_P1_P10_FINAL_AUDIT.md](productization/PRODUCTIZATION_P1_P10_FINAL_AUDIT.md). **P1–P10 is a UI/UX programme; finishing it does not complete a canonical product phase, and commercialization is not started.**  
Living trail: [WORK_TRAIL.md](WORK_TRAIL.md). Blueprint: [MASTER_BLUEPRINT.md](MASTER_BLUEPRINT.md). Pack: [CANONICAL_PRODUCT_PACK.md](CANONICAL_PRODUCT_PACK.md).

Purpose: distinguish what exists in code from what has been proven as a product. A route, table, migration, placeholder screen, or successful build does **not** equal phase completion.

---

## Canonical product position

| Phase | Status |
| --- | --- |
| **1 — Foundation** | **LOCAL EXIT GATE PROVEN (2026-08-20)** — production env still an ops blocker for live intake |
| **2 — Real-Document Historical Pilot** | VERIFY 2B PASS WITH NONBLOCKING GAPS + Prompt 2C schema + **VERIFY 2C PASS** ([VERIFY2C_ACCEPTANCE.md](pilot/VERIFY2C_ACCEPTANCE.md)). **Exit UNMET:** F1 shows 0 workbench-verified facts and 0 `NEEDS_REVIEW`. OCR + corpus depth still open; the 25 MB gate never existed. |
| **3 — Historical Ingestion & Migration** | **PASS** — Prompt 3 + **VERIFY 3 PASS 26/26** ([VERIFY3_ACCEPTANCE.md](pilot/VERIFY3_ACCEPTANCE.md)) + **F1 37/37** ([F1_PRODUCTION_INGESTION_ACCEPTANCE.md](functionality/F1_PRODUCTION_INGESTION_ACCEPTANCE.md)). DOCX wired; OCR live only with `MISTRAL_API_KEY` and never yet run on real evidence. |
| **4 — Contracts / Compliance** | Prompt 4 exit proven in app + acceptance, plus **P10 portfolio / renewal-rebid command center**; still thin vs a real awarded corpus — 12 contracts, **3** with a verified end date, **0** with an NTE or PO |
| **5 — Buyer / Competitor / Market / Win-Loss** | **Prompt 5 exit proven** — still corpus-thin; no fabricated market share |
| **6 — Search / Ask / Reports / Automation** | EARLY/PARTIAL |
| **7 — Pricing Intelligence** | Prompt 7 exit + **P7 workbench polish**; **VERIFY 7's committed 29/29 is stale** (19/22 at clean `HEAD` — outdated fixtures/grep, not a behaviour regression) |
| **8 — Response Builder / Submission / Result** | **PASS** (Prompt 8 + VERIFY 8 **23/23, re-earned 2026-08-21** after its unsourced-contract fixture was replaced) + **P8 submission / outcome / handoff polish** |

The product remains in the transition from Foundation into the real-document Historical Pilot. That is the honest current maturity position.

---

## Phase notes

### Phase 1 — Foundation

**Proven locally (Prompt 1):** Auth + organizations/memberships + `organization_id` RLS (48/48 including Storage isolation and same-org integrity); SHA-256 registry/versions; batches; extraction runs; staging facts default `AI_EXTRACTED`; append-only source evidence; verification events; validation exceptions; Zod/Pydantic contracts; Vercel Workflow + JobPort; FastAPI processor (9 pytest); PDF review workbench; lint/typecheck/build. See [PHASE1_FOUNDATION_AUDIT.md](PHASE1_FOUNDATION_AUDIT.md). Role enum is stored but not enforced in UI — not a tenancy hole. **Do not treat this as Historical Pilot completion.**

### Phase 2 — Real-Document Historical Pilot

A public-first L&P corpus strategy has been opened because the complete private historical library is not yet available. The correct next proof is **real L&P procurement evidence**, not more fixture/demo expansion.

Queue / verified primary sources (not yet ingested): [HISTORICAL_PILOT.md](HISTORICAL_PILOT.md), [pilot/PUBLIC_PACKAGE_QUEUE.md](pilot/PUBLIC_PACKAGE_QUEUE.md).

Until a package completes source preservation → extraction → staging → validation → human verification → canonical promotion, it is **not** counted as a verified historical package. VERIFY 2B records **7 A/B** packages through that path with nonblocking gaps; Prompt **2C** expanded schema only where [PILOT_GAP_REPORT.md](benchmarks/PILOT_GAP_REPORT.md) proved need. Exit target ~20–30 packages remains unmet.

### Phase 3 — Historical Ingestion / Migration

Intake, checksum/versioning, parsing/extraction, staging, verification, and bulk-oriented pieces may exist, but must be validated and revised using the real-document pilot before being called production-ready.

### Phase 4 — Contracts / Compliance

**Canonical Phase 4 exit (Prompt 4):** Portfolio + Renewals + Compliance secondary nav; Contract workspace Overview | Service Plan | Commercial Terms | Changes | Renewal wired to verified tables (`contract_service_plans`, POs, federal IDs, amendments, alerts, compliance). Alert buckets 180/120/90/60/30/EXPIRED from `verified_end_on` only. See [PHASE4_ACCEPTANCE.md](PHASE4_ACCEPTANCE.md). Still corpus-thin for real awarded instruments — never fabricate absent terms.

**As of P10:** Renewals and Compliance are reached from in-area cross-links, not sidebar peers (P1's demotion stands). The five workspace tabs are unchanged in structure and now share one model with the portfolio list. **Corpus-thin is now quantified:** 12 contracts, of which **3 carry a `verified_end_on`** (1 active, 2 expired) and **0 carry an award NTE or a purchase order** — so the 180/120/90/60/30 queue and Active Contract Value are proven by acceptance script only and have never been exercised against real data.

### Phase 5 — Intelligence

**Canonical Phase 5 exit (Prompt 5):** Global Intelligence Buyers | Competitors | Market | Pricing | Win/Loss | Content | Reports wired to verified tables. Buyers = procurement portfolio (not CRM). Market tiles exclude document counts. Content shows reuse `APPROVED | REVIEW_REQUIRED | DO_NOT_USE | SUPERSEDED`. Pursuit Overview surfaces the cross-corpus summary — as of P5 that lives in its Buyer intelligence and Competitive intelligence sections rather than a standalone panel. See [PHASE5_INTELLIGENCE_ACCEPTANCE.md](PHASE5_INTELLIGENCE_ACCEPTANCE.md). Still corpus-thin — never invent share or causation.

### Phase 6 — Search / Ask / Reports / Automation

**Canonical Phase 6 exit (Prompt 6):** Header `[Find or Ask GPT...]` with LOCATE | ASK/ANALYZE | REPORT. Purpose-aware hybrid retrieval (SQL/FTS/pgvector); eight report generators; Answer/Sources/Data Scope/Limitations/View Source contract; `automation_events` via Supabase pg_cron + Vercel Cron digest; human gates never bypassed. See [PHASE6_ASK_REPORTS_AUTOMATION_ACCEPTANCE.md](PHASE6_ASK_REPORTS_AUTOMATION_ACCEPTANCE.md). Still corpus-thin — insufficient evidence returns the canonical refusal sentence.

### Phase 7 — Pricing

**Canonical Phase 7 exit (Prompt 7):** Pursuit → Pricing Glide workbench with five truths (requested / internal cost / submitted / awarded / current), dynamic structure grain, cost model, include/exclude comparables with reasons, observed range + cost floor + target-margin threshold + confidence, and human-required final bid (`pricing_decisions`). Intelligence → Pricing is cross-corpus only and points operators back to the Pursuit workbench for live bids. See [PHASE7_PRICING_ACCEPTANCE.md](PHASE7_PRICING_ACCEPTANCE.md).

**VERIFY 7:** the committed run records **PASS (29/29)** — blank comparable reasons rejected at DB; Glide five-truth matrix exposes per-truth `source_fact` verification links ([VERIFY7_ACCEPTANCE.md](pilot/VERIFY7_ACCEPTANCE.md)). **That PASS is now stale.** Re-run on a clean `HEAD` worktree (2026-08-21) it is **19/22**, and `test:phase7-pricing` is **12/13**, for two reasons unrelated to product behaviour: both suites insert `pricing_lines.awarded_rate` with no `awarded_source_fact_id`, which the later trust trigger (`20260821120000`, commit `259fd9f`) correctly rejects; and one check greps `actions.ts` for `decided_by: approve ? user.id`, which became `userId` in commit `c1747dd`. The rules under test still hold — **the fixtures and the grep are out of date, and re-earning VERIFY 7 is its own task.**

### Phase 8 — Response / Submission / Result

**Canonical Phase 8 exit (Prompt 8):** Pursuit-central Requirements matrix → Tiptap Response workspace (left/center/right) with evidence states, reuse gates (DO_NOT_USE blocked for drafting), progress counters, configurable approvals, submission packet/checklist/exports, result capture (incl. NO_AWARD), contract-on-win, Intelligence revalidation. No global Proposal app. See [PHASE8_RESPONSE_ACCEPTANCE.md](PHASE8_RESPONSE_ACCEPTANCE.md). Still corpus-thin for rich grounded drafts.

**VERIFY 8:** **PASS** (23/23). End-to-end pre-award flow on Arlington/Lottery-shaped package; steps 1–22 all PASS. See [VERIFY8_ACCEPTANCE.md](pilot/VERIFY8_ACCEPTANCE.md). Two of those steps had regressed to FAIL after the trust migration because the fixture inserted a contract with no `source_fact_id`; as of P8 (2026-08-21) both steps assert the **unsourced insert is refused** and then insert via a HUMAN_VERIFIED award fact, and step 16 additionally proves the new `submission_packets_submitted_requires_actor` constraint by attempting an anonymous submission first. **The PASS is on evidence, not on a weakened gate.**

---

## Current correct next work

1. Grow verified historical corpus (~20–30 packages) — live org now **22 packages / 15 A/B harness-complete**. **F1 audit: 195 of 204 `HUMAN_VERIFIED` facts are harness stamps and 0 are workbench-attributed, so the count is automation output, not human verification. The lower bound is NOT met in trust terms.**  
2. Route a real reviewer through the verification workbench — `NEEDS_REVIEW` is currently **0**, so nothing is queued for a human.  
3. Confirm Vercel prod processor + set `ASK_MODEL` / `MISTRAL_API_KEY` as needed. OCR has never run on real scanned evidence.  
4. Keep [WORK_TRAIL.md](WORK_TRAIL.md) honest.  

**F2 functional build (2026-08-21):** Public Procurement Opportunity Discovery Engine — extends P4 provider abstraction (no duplicate). `getOpportunity` / `getDocuments` / `healthCheck`; `public_sources.status` + `content_hash`; org-scoped `opportunity_search_profiles` + daily CRON_SECRET sync that upserts **live** provider hits only (fixture/sample mode skipped / fail closed; never invents, never `HUMAN_VERIFIED`); Discover manual paste + profiles UI; Start Pursuit trust unchanged. `test:f2-opportunity-engine` 16/16. Live SAM.gov still unproven against a real response. See [F2_PUBLIC_OPPORTUNITY_ENGINE_ACCEPTANCE.md](functionality/F2_PUBLIC_OPPORTUNITY_ENGINE_ACCEPTANCE.md).

**F1 functional build (2026-08-21):** Production ingestion / OCR / real-corpus hardening. Fixed the `OCR_REQUIRED:` overwrite bug (the parse-and-extract catch-all was blanking the Data Ops OCR badge — it had already mislabelled the live SRC-19 row), added error class + human reason on failed jobs, fixed the corpus funnel `extracted` count and expanded it to 10 separately reported stages with script-stamp attribution on `HUMAN_VERIFIED`, and aligned routing/README/env docs to the wired DOCX + key-gated OCR reality. `test:f1-ingestion` 37/37, processor pytest 45, VERIFY 3 26/26, VERIFY 2A 132/132, foundation + phase3 suites PASS, lint/typecheck/build clean. **There is no 25 MB gate anywhere in code** — intake is 50 MB end to end and the processor imposes no byte limit. See [F1_PRODUCTION_INGESTION_ACCEPTANCE.md](functionality/F1_PRODUCTION_INGESTION_ACCEPTANCE.md).

**P2 productization (2026-08-21):** Data Ops hardening complete. Intake UX preflight validation, processing queue lifecycle_error badges/filters, OCR_REQUIRED semantics, re-extract guard for HUMAN_VERIFIED facts, verification workbench keyboard/optimistic/auto-advance, exceptions disposition notes, corpus funnel report script. See [P2_REAL_CORPUS_DATA_OPS_ACCEPTANCE.md](productization/P2_REAL_CORPUS_DATA_OPS_ACCEPTANCE.md).

**P4 productization (2026-08-21):** Public opportunity discovery complete. Pursuits gained Discover | Watchlist | Active | Submitted | Closed tabs; `public_sources` table with RLS; SAM.gov adapter (live with `SAM_GOV_API_KEY`, otherwise clearly labeled `FIXTURE-SAM-*` sample data with an honesty banner); Watch / Dismiss / Start pursuit. Discovery results are **not persisted on view**, public notice facts land **`AI_EXTRACTED`** only, and there is **no AI fit score**. Starting a pursuit does not ingest the solicitation — documents and verified truth still come from Data Ops. **The live SAM.gov request path has never been exercised against a real response.** Fixture notices are sample data and count toward nothing in the Historical Pilot. See [P4_OPPORTUNITY_DISCOVERY_ACCEPTANCE.md](productization/P4_OPPORTUNITY_DISCOVERY_ACCEPTANCE.md). F2 extends this surface (status, profiles, sync, healthCheck) without replacing it.

**P7 productization (2026-08-21):** Pricing workbench polish complete. The Pursuit → Pricing Glide matrix now pins `labor_category · site_or_post · unit`, groups and colour-bands each of the five truths, formats rates as currency (absent stays `—`, never `0.00`), states per-truth coverage (`Buyer awarded 8/8` beside `Buyer requested 0/8`) in both the sticky workbench header and the grid legend, navigates from a Source cell to `/ingestion/verification/<documentId>`, and opens a provenance sheet from any rate cell naming that truth's provenance and the **only human path that can change it**. Comparables became a dense include/exclude table carrying source pursuit, why-comparable, grain, which truths are sourced, and recency; every range readout states `n=…` and no bar is drawn below **n = 3**. Final bid reads **FINAL PRICE — HUMAN DECISION REQUIRED** with a `No AI approval path` badge. Buyer format is read off real line grains, with an explicit *supported-but-not-observed* list so no requested cell is invented. **No schema change, no migration, no new dependency (Tremor declined), and no new writer to `pricing_lines`.** The grid is **read-only by design**: it declares no `onCellEdited` and sets `onPaste={false}`, because a grid edit would create a rate with no `source_fact_id` — the exact thing the trust trigger rejects — and `internal_cost_rate` is derived by `saveCostModel` from a per-category cost build-up, not typed per line. Honest limits: client-side validation is convenience and the server actions/DB constraints remain the enforcing boundary; range bars were proven by script only because Arlington has 0 included comparables; structure hints are keyword matching over line grains; recency is `pricing_lines.updated_at`, not the age of the award document. See [P7_PRICING_WORKBENCH_ACCEPTANCE.md](productization/P7_PRICING_WORKBENCH_ACCEPTANCE.md).

**P9 productization (2026-08-21):** Full Intelligence Workbench polish complete. The seven secondary views (Market, Buyers, Competitors, Pricing, Win/Loss, Content, Reports) were productized on top of the **unchanged** Phase 5 loaders and Phase 6 Ask/Reports engines — **no migration, no new dependency, no second chatbot, no second research engine.** Three pure modules carry the rules so seven views cannot disagree about them: `lib/intelligence/ask-launch.ts` makes `/intelligence/ask` the only Ask path and `buildAskHref` the only builder (fixed param order, an unknown purpose falling back to the mode default so a chip cannot widen retrieval, a fixed purpose per view, and `Object.hasOwn` on the view map so an inherited `Object` key is not a view); `lib/intelligence/observations.ts` renders the honesty strip text on every view and makes a tile **impossible to construct without its own `n=` and source table**, labels every value `OBSERVED` or `INFERENCE`, and gates the win rate at `MIN_WIN_RATE_SAMPLE = 20`; `lib/intelligence/recompete-radar.ts` builds the new **Recompete Radar** from existing `awards`/`contracts`/`contract_options`/`renewals`/`opportunities`/`clients`/`win_loss_reviews` only. **There is no market-share KPI and no document-count KPI anywhere on the workbench**, and no median, typical or expected rate is derived — a pricing span is `min – max` with `n` stated. **Expected rebid restates `contracts.verified_end_on` or the earliest recorded option exercise-by / renewal notice date and names the field it used; it is never inferred from a term length, a typical cycle, or an award date**, and absent both the row says the timing is unknown. **L&P-held contracts are never market radar rows** — they are the internal renewal queue and the Market view links `/contracts/renewals` as a separate list with its own scope note. Radar filters cover only fields the schema already carries (`opportunities.service_type`, `site_location`, an expected-rebid window), offer only values the corpus actually has, and **drop rows whose rebid timing is unknown rather than assuming one**. Win rate is `WON ÷ (WON + LOST)` with `NO_BID`/`CANCELLED`/`NO_AWARD`/`PENDING` excluded from both sides; below 20 decided pursuits the percentage is **withheld** with the reason and the current decided count, and the rate is always computed over the corpus, never the filtered table. The Ask page honors incoming `mode`/`purpose`/`q` and renders a context banner stating that carried context is **provenance only and did not narrow retrieval**; the dual-rail agent was not rebuilt. `/intelligence/buyers` is now a permanent redirect to `/intelligence/clients`, with the data-model registry and the home Market snapshot corrected. Honest limits: all four live market radar rows are `PARTIAL` on the strength of an award-document source alone — the incumbent is **not** named and `verified_end_on` is absent on every one, so every `expected rebid` reads `unknown` and **the radar cannot yet plan a pursuit calendar**; the win-rate display branch has never been reached on this corpus (1 review, 0 decided) and is proven by script only; the live pricing span reads `6.00 – 4,722,008.00 (n=31)` because `pricing_lines` mixes hourly rates with annual totals in one column — honest with its `n`, but **the promoter's rate-versus-total mapping needs fixing** before the number means anything; Content tiles count the current result set rather than the corpus; and P9 polished the eight report kinds' catalog, cutoff and Ask launch without auditing the generators' internals. See [P9_INTELLIGENCE_WORKBENCH_ACCEPTANCE.md](productization/P9_INTELLIGENCE_WORKBENCH_ACCEPTANCE.md).

**P8 productization (2026-08-21):** Submission + Outcome + Contract Handoff polish complete. Submission readiness is now **computed** by one pure module (`lib/opportunity/submission-readiness.ts`) over the stored checklist, the enabled approval layers, the latest pricing decision, `computeResponseProgress` and the packet logistics, using six statuses of which only `COMPLETE` and `NOT_APPLICABLE` are settled — **`UNKNOWN` is outstanding, so a pursuit with no checklist reads `Readiness unknown`, never ready.** The workbench shows a sticky strip (overall state, `n/m required (p%)`, all six counts, and a named blocking list), checklist status badges, a **read-only** approvals mirror that sends decisions back to Response, and an Outputs section that names what each artifact actually is: HTML for print-to-PDF because *the app does not render PDFs*, *Word-compatible HTML (.doc) — not a native DOCX/OOXML file and no DOCX writer exists in this codebase*, and *there is no Google Docs integration: nothing is created, pushed, or synced*. **Marking submitted is now a distinct human-only action:** `saveSubmissionPacket` lost every path to `submitted_at` and to stage `SUBMITTED`, and the new `markSubmissionSubmitted` recomputes readiness server-side, requires an explicit authorization tick, and writes `submitted_by` from the session — never from the form. The additive migration `20260821200000` adds `submitted_by` / `submission_url` / `submission_instructions` and a `NOT VALID` check that no timestamp may exist without an actor. The Result panel separates **buyer-documented** fields from **internal-only** analysis, lists the pursuit's HUMAN_VERIFIED facts tagged `Award-shaped` vs `Other`, and states the contract gate before the click. `createContractFromWin` stays idempotent, cites the verified fact, links an `awards` row with a notice string only, and **no longer infers `WON` from the click** — a verified award fact can name a competitor, so a human-recorded WON outcome is now required. Honest limits: marking submitted is a **record, not a submission** (nothing uploads, emails or delivers); `submitted_at` is operator-supplied and may be back-dated; the actor constraint is `NOT VALID`, so pre-P8 anonymous timestamps are not retro-attributed; with no approval layer enabled the "Internal approvals" tick is an attestation, not a recorded approval; readiness cannot see a buyer form nobody added to the checklist; and `AWARDISH_FACT_RE` is keyword matching that gates *which fact may be cited*, while the human outcome and `contracts_require_verified_fact` remain the real gates. See [P8_SUBMISSION_RESULT_HANDOFF_ACCEPTANCE.md](productization/P8_SUBMISSION_RESULT_HANDOFF_ACCEPTANCE.md).

**P6 productization (2026-08-21):** Requirement-driven Response workspace complete. The Response tab now gates action on the evidence state of the selected requirement: a sticky progress header over the existing `computeResponseProgress` counters plus an **approved-share** percentage (unapproved drafts are not progress); left-nav filters **All | To Do | Input Required | Review | Approved | Mandatory | Scored** with live counts; the selected requirement showing exact text, § / page, mandatory, weight, attachment + form, evidence state, matrix status and draft status; a `ui/sheet` source panel carrying saved `sources_used` and retrieved passages with verification links; and a Tiptap editor with a selection bubble menu, a slash-block stub, debounced autosave, and Ctrl/Cmd+S. Retrieval now **follows the selection** instead of describing `requirements[0]` for every row. **No schema change, no new pursuit tab, no new dependency, and Phase 8 business logic was not rebuilt.** Trust: `DO_NOT_USE` / `SUPERSEDED` / unretrieved chunks are rejected *above* the operator override so nothing can bypass them; Generate is disabled while evidence is `L_AND_P_INPUT_REQUIRED` (Request L&P input stays enabled) and the override keeps the never-invent warning on screen; `REVIEW_REQUIRED` requires explicit source selection; an approved response is locked against silent regeneration; and `approve=1` is produced only by the explicit approve intent, so **autosave can never approve**. Honest limits: the gate is client-side (the server action remains the enforcing boundary), selected sources confirm human review rather than filtering the server's retrieval, Compare is session-scoped because there is no response version history, and the `REVIEW_REQUIRED` / `VERIFIED_DRAFT_AVAILABLE` generate paths were proven by the acceptance script rather than in the browser — the available corpus is too thin. See [P6_RESPONSE_WORKSPACE_ACCEPTANCE.md](productization/P6_RESPONSE_WORKSPACE_ACCEPTANCE.md).

**P5 productization (2026-08-21):** Pursuit Overview + Bid Strategy complete. The Overview is now eleven read-first sections (solicitation summary, scope, how this is scored, bid/no-bid, buyer, competitive, prior L&P experience, compliance readiness, risks, bid strategy, next actions) composed from one RLS-scoped bundle; pricing planning and the metadata form are demoted into collapsed blocks. **No schema change, no new pursuit tab, no LLM call, no LangGraph.** Bid strategy is a pure function over this pursuit's own rows: every bullet carries an openable citation, absent evidence produces a named withheld line, and zero bullets returns **INSUFFICIENT** rather than a thinner answer. No score, win rate, market share, probability, or causal claim is generated; **the incumbent is never inferred** — an award row with a null winner says so. Bid/no-bid stays a human decision the platform records and never sets. Compliance readiness is honestly **Unknown** pre-award because `compliance_items` hang off contracts, not pursuits. See [P5_PURSUIT_STRATEGY_ACCEPTANCE.md](productization/P5_PURSUIT_STRATEGY_ACCEPTANCE.md).

**P10 productization (2026-08-21):** Contract portfolio + renewal/rebid command center complete, on the **unchanged** Phase 9 contracts schema and `refresh-contract-alerts` cron — **no migration, no schema change, no new dependency.** One pure module (`lib/contracts/portfolio-model.ts`) now owns status, buckets, values, next action and risk, so the portfolio list, the renewal queue and the per-contract Overview cannot disagree; `deriveContractStatus` moved out of the loader and is re-exported. Four rules are enforced in code rather than described: a date is **verified or absent** (a contract with no `verified_end_on` is undated, never assumed active, and produces no bucket); a value is a **named instrument or an em dash** (`NTE_CEILING` from `awards.amount_nte` and `PO_OBLIGATED` from purchase orders are never summed, amendments carry no amount so they never move a value, and the portfolio total is **withheld** unless every in-scope contract has one); the portfolio is **not the Market Recompete Radar** and the two lists can never share a row; and **nothing on these pages acts** — buckets and readiness are advisory. `/contracts` gained five `n=`-bearing KPI tiles, a Buyer/Contract#/Status/Original/Current/Start/Expiration/Options/NextAction/Risk table, and five lanes that **partition** the portfolio. Changes became one append-only `Original → Amendment → Modification → Option → Renewal` timeline with per-entry source links; Commercial Terms labels the four truths and states instrument precedence as a **reading order, not an automatic overwrite**; Renewal carries advisory compliance readiness where an empty list is **unknown, not clear**. The CTA is **Start Rebid Pursuit** and `cloneRebidFromContract` **copies no pricing**. `AutomationAuditStrip` states the cron's entire scope: it recomputes buckets and deletes stale ones, and writes no term, exercises no option, sends no notice, and approves nothing. **P1's demotion of Renewals/Compliance out of the sidebar was respected, not reverted** — discoverability is in-area cross-links, and the stale `phase4-contracts` `[ui] ContractsNav` grep was rewritten to assert that demotion positively. **Honest limit: the surface is correct and nearly empty** — 9 of 12 live contracts have no verified end date, every bucket except `EXPIRED` is zero, none carries an NTE or a purchase order, so Active Contract Value has only ever rendered in its withheld state and every value cell is a dash. Options-remaining cannot be built because exercised status is not recorded. See [P10_CONTRACT_RENEWAL_REBID_ACCEPTANCE.md](productization/P10_CONTRACT_RENEWAL_REBID_ACCEPTANCE.md) and the programme-level [PRODUCTIZATION_P1_P10_FINAL_AUDIT.md](productization/PRODUCTIZATION_P1_P10_FINAL_AUDIT.md) — which records that P1–P10 is a **UI/UX programme**, that the Historical Pilot has **not** exited (15 A/B packages of ~20–30), that VERIFY 5 / 6 / 7 currently return **verdict FAIL** on stale fixtures versus the Phase 9 trust triggers, and that **Stripe / MCP / agents are not started**.

**P3 productization (2026-08-21):** Executive Home + Action Center complete. Replaced `/overview` with owner command center: KPI strip (10 real metrics), Needs Attention queue, Pipeline table, Win/Loss snapshot, Contract alerts by bucket, Market intelligence. All RLS-scoped queries; no fake metrics; honest empty states. See [P3_EXECUTIVE_HOME_ACCEPTANCE.md](productization/P3_EXECUTIVE_HOME_ACCEPTANCE.md).

**Prompt 9 / VERIFY 9 (2026-08-20):** Fix pass → **READY WITH NONBLOCKING LIMITATIONS**. Deferred: corpus acquisition ~20–30, Vercel login + prod processor, `ASK_MODEL`. See [VERIFY9_ACCEPTANCE.md](pilot/VERIFY9_ACCEPTANCE.md).

---

## Known product / UX reconciliation required

**Final IA:**

- Global = Home | Pursuits | Intelligence | Contracts | Data Ops | Settings  
- Pursuit = Overview | Requirements | Pricing | Response | Submission | Result  
- Contract = Overview | Service Plan | Commercial Terms | Changes | Renewal  
- Intelligence = Buyers | Competitors | Market | Pricing | Win/Loss | Content | Reports  
- Data Ops = Intake | Processing | Verification | Exceptions | Historical Migration  
- Ask GPT = persistent global header capability  

Any older navigation that exposes Ingestion, Proposals, Data Quality, Requirements, Pricing, Renewals, Compliance, Buyers, Market, Reports, etc. as **unrelated peer global modules** must be reorganized rather than treated as canonical IA.

**Code today (P1 UX Foundation):** authenticated shell matches canonical IA — global **Home | Pursuits | Intelligence | Contracts | Data Ops** as **five single sidebar links** (Intelligence/Data Ops no longer expandable submenus) with Settings in the footer; Ask GPT in the header; Pursuit tabs **Overview | Requirements | Pricing | Response | Submission | Result**; Contract tabs **Overview | Service Plan | Commercial Terms | Changes | Renewal**; horizontal section tabs appear **after entering** Intelligence or Data Ops. Shell primitives: `PageHeader`, `WorkspaceHeader`, `EmptyState`, `CollectionPage` under `components/shell/`. CONTRACTS_TABS demoted Renewals/Compliance (pages still work via breadcrumb back to Portfolio). Legacy URLs (`/proposals`, `/ingestion/*`) remain as remounts/redirects, not peer global apps. Intelligence / Ask / Response surfaces stay **early/partial / unvalidated** until the Historical Pilot.

---

## Implemented / proven vs unvalidated labels

- **IMPLEMENTED + PROVEN** — passed relevant tests and proven on the intended real workflow/data  
- **IMPLEMENTED, UNVALIDATED** — code exists but has not been proven on the target corpus/workflow  
- **PARTIAL** — meaningful pieces exist but the end-to-end capability is incomplete  
- **DOCUMENTED ONLY** — specified but not built  
- **NOT STARTED** — no meaningful implementation  

---

## Non-negotiable audit warnings

- RLS/tenancy success is Foundation, not completion of the Historical Pilot.  
- A green build does not prove procurement correctness.  
- A parser that works on fixtures does not prove real L&P extraction quality.  
- A table/route does not prove a business capability.  
- A dashboard with empty/fake numbers is not intelligence.  
- An FTS result page is not the finished Ask GPT experience.  
- A planning pricing panel is not the finished Glide pricing workbench.  
- An opportunity/proposal shell is not the finished Response Builder.  
- Public non-L&P documents may validate document types, but they are **test corpus**, not L&P historical truth.  

---

## Ops / environment blockers (still true)

1. Confirm Vercel env + signed-in org for real intake.  
2. Processor running for parse → `extracted_facts`.  
3. Allen full board packet measures **31.1 MB / 50 MB** and ingests successfully (VERIFY 3). The old "25 MB blocked SRC-03" claim was stale — **no 25 MB gate has ever existed in code**.  
4. `MISTRAL_API_KEY` unset, so `ocr_ready` is false and scanned PDFs fail closed as `OCR_REQUIRED`. SRC-19 has never been OCR'd.  
5. The local processor has no `--reload`; restart it from `services/processor` after processor changes or it silently serves stale code (observed 2026-08-21, leaving a document stuck in `PARSING`). 

Opportunity migrations `20260820300000` / `310000` / `320000` plus VERIFY 1 hardening `20260820400000` were applied to remote Postgres on 2026-08-20. Migrations `20260821160000`, `20260821170000`, P4 `20260821180000_p4_public_opportunity_discovery`, and P8 `20260821200000_p8_submission_authorization` were applied on 2026-08-21 — the latter confirmed live with `submission_packets_submitted_requires_actor` present and `convalidated = f` (enforced on new writes, not retro-validated).  

Detail: [WORK_TRAIL.md](WORK_TRAIL.md).
